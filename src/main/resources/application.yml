spring:
  datasource:
      url: jdbc:mysql://127.0.0.1:3306/hanaup?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
      username: root
      password: ejkimysjong35!
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.orm.jdbc.bind: trace`

server:
  port: 8081




